export type Point = { x: number; y: number };

export type Dimensions = { height: number; width: number };

export type Point3D = { x: number; y: number; z: number };

export type Images = { front: string; back: string };
