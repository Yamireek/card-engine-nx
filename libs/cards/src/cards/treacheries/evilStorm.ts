import { treachery } from '@card-engine-nx/state';

export const evilStorm = treachery(
  { name: 'Evil Storm' }
  // When Revealed: Deal 1 damage to each character controlled by each player with a threat of 35 or higher.
);
